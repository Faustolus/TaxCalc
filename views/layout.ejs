<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="https://code.jquery.com/ui/1.12.1/themes/base/jquery-ui.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.3/css/all.min.css">
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://code.jquery.com/ui/1.12.1/jquery-ui.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.bundle.min.js"></script>
    <style>
        body {
            font-family: Arial, sans-serif;
            background-color: #f0f2f5;
            color: #333;
            margin: 0;
            padding: 0;
        }
        .container {
            max-width: 900px;
            margin: 50px auto;
            padding: 20px;
            background-color: #fff;
            border-radius: 8px;
            box-shadow: 0 0 10px rgba(0, 0, 0, 0.1);
        }
        .form-title {
            text-align: center;
            margin-bottom: 20px;
        }
        .form-title h1 {
            margin: 0;
            font-size: 24px;
        }
        .form-title p {
            margin: 5px 0 0;
            color: #666;
        }
        .form-section {
            margin-bottom: 30px;
        }
        .form-section h2 {
            font-size: 18px;
            margin-bottom: 10px;
            border-bottom: 2px solid #eee;
            padding-bottom: 5px;
        }
        .form-group {
            display: flex;
            flex-wrap: wrap;
            margin-bottom: 15px;
        }
        .form-group label {
            width: 100%;
            margin-bottom: 5px;
            display: flex;
            align-items: center;
        }
        .form-group label i {
            margin-left: 5px;
            font-size: 14px;
        }
        .form-group input, .form-group select {
            width: 100%;
            padding: 10px;
            border: 1px solid #ccc;
            border-radius: 4px;
            margin-bottom: 10px;
        }
        .form-group input[type="submit"], .form-group input[type="button"], .form-group input[type="reset"] {
            width: auto;
            margin-right: 10px;
            cursor: pointer;
            background-color: #007bff;
            color: #fff;
            border: none;
        }
        .form-group input[type="button"], .form-group input[type="reset"] {
            background-color: #6c757d;
        }
        .form-group input[type="submit"]:hover, .form-group input[type="button"]:hover, .form-group input[type="reset"]:hover {
            opacity: 0.9;
        }
        .hidden {
            display: none;
        }
        @media (min-width: 576px) {
            .form-group.col-sm-6 {
                flex: 0 0 48%;
                max-width: 48%;
            }
        }
        .capital-letters-group {
            display: flex;
            justify-content: space-between;
            align-items: center;
            width: 100%;
            margin-top: 10px;
        }
        .capital-letter-item {
            display: flex;
            align-items: center;
            width: 25%;
        }
        .capital-letter-item input[type="checkbox"] {
            margin: 0;
            padding: 0;
            width: auto;
            height: auto;
        }
        .capital-letter-item label {
            margin: 0 0 0 5px;
            padding: 0;
            line-height: 1;
            font-size: 14px;
        }
        .form-check-inline {
            display: inline-flex;
            align-items: center;
        }
        .form-check-inline input[type="radio"] {
            margin-right: 5px;
        }
        .form-group > label + i {
            margin-left: 5px;
        }
    </style>
</head>
<body>
    <!-- Navigation Bar -->
    <nav class="navbar navbar-expand-lg navbar-light bg-light">
        <a class="navbar-brand" href="#">TaxCalc</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Navigation
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        <a class="dropdown-item" href="#" data-target="personalData">Personal Data</a>
                        <a class="dropdown-item" href="#" data-target="bank">Bank Information</a>
                        <a class="dropdown-item" href="#" data-target="tax">Tax Information</a>
                        <a class="dropdown-item" href="#" data-target="children">Children Information</a>
                        <a class="dropdown-item" href="#" data-target="incomeTaxCertificate">Income Tax Certificate</a>                        
                        <a class="dropdown-item" href="#" data-target="capitalBuildingBenefits">Capital Building Benefits</a>

                    </div>
                </li>
            </ul>
        </div>
    </nav>

    <div class="container" id="mainContainer">
        <div id="content">
            <!-- Dynamic content will be loaded here -->
        </div>
    </div>
    <div id="dynamic-content" class="hidden"></div>

    <script>
        $(document).ready(function() {
            // Function to load content dynamically
            function loadContent(target) {
                $.get('/' + target, function(data) {
                    $('#content').html(data);
                });
            }

            // Set up navigation click handlers
            $('.dropdown-item').click(function(e) {
                e.preventDefault();
                const target = $(this).data('target');
                loadContent(target);
            });

            // Load the default form (e.g., personal data) on page load
            loadContent('personalData');
        });
    </script>
</body>
</html>